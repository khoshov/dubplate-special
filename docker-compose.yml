

services:
  web:
    build: .
    container_name: django_app
    ports:
      - 8000:8000
    volumes:
      - .:/app
  postgres:
    image: postgres
    env_file:
      - .env
    ports:
      - "5432:5432"


